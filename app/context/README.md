# Context

This folder contains **React Contexts** for managing global state.

## Examples
- `AuthContext.tsx` → Provides authentication state across the app.
- `ThemeContext.tsx` → Provides dark/light theme settings.

> Contexts should be wrapped in `providers/` if they are applied globally.
