# Hooks

This folder contains **custom React hooks** that are reused globally.

## Examples
- `useAuth.ts` → Provides authentication state.
- `useFetch.ts` → Fetch wrapper with loading/error states.

> Feature-specific hooks should go inside their respective `features/<feature>/hooks/` folder.
