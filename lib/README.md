# Lib

This folder contains **utility functions, API clients, and external integrations**.

## Examples
- `prisma.ts` → Prisma database client.
- `apiClient.ts` → Axios/fetch wrapper for API calls.
- `validators.ts` → Shared validation functions.

> Keep this folder **framework-agnostic** (no React code).
