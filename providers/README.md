# Providers

This folder contains **global providers** that wrap the Next.js app.

## Examples
- `ThemeProvider.tsx` → Applies dark/light theme.
- `AuthProvider.tsx` → Provides authentication context.

> Use this to centralize app-wide providers, instead of spreading them across pages.
