# Public

This folder contains **static assets** served directly at runtime.

## Examples
- `images/` → Static images.
- `favicon.ico` → Browser favicon.
- `robots.txt` → SEO-related file.

> Access assets with `/filename` (e.g., `/favicon.ico`).
